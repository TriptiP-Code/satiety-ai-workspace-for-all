apiVersion: v1
kind: Service

metadata:
  name: satiety-backend-service
  labels:
    app: satiety-backend

spec:
  selector:
    app: satiety-backend

  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 32364

  type: NodePort