apiVersion: v1
kind: Service

metadata:
  name: satiety-backend-service

spec:
  selector:
    app: satiety-backend

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000

  type: NodePort