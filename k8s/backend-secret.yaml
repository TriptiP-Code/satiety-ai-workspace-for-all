apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
type: Opaque
stringData:
  # Create the real secret in your deployment environment; do not commit credentials.
  SUPABASE_SERVICE_ROLE_KEY: REPLACE_AT_DEPLOY_TIME
  SUPABASE_ANON_KEY: REPLACE_AT_DEPLOY_TIME
  JWT_SECRET: REPLACE_AT_DEPLOY_TIME
